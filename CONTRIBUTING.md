# 贡献指南

感谢你参与 TihuTest 的改进。

## 问题反馈

请先搜索已有 Issues。提交问题时提供：

- 问题现象、预期行为与复现步骤
- 使用的提交版本、浏览器或 Docker 环境
- 相关日志或截图；HTML 展示问题可附最小复现文件

日志和附件中请移除管理员口令、访问令牌及其他私人信息。

## 提交修改

1. Fork 仓库并创建功能分支。
2. 按 README 配置本地环境，使用锁文件安装依赖：`npm ci`。
3. 围绕一个问题提交修改，并同步相关文档。
4. 提交 Pull Request，描述修改前后的行为及验证结果。

应用代码修改后运行 `npm run build`，并检查受影响的实际流程。仅修改文档时，检查命令、链接、路径和排版即可。Docker 相关修改应注明是否完成镜像构建和容器启动验证。

请勿提交 `.env`、`.env.local`、`.openai/`、本地数据库、上传文件、构建产物或 `node_modules/`。配置示例请写入 `.env.example`，使用占位值。
